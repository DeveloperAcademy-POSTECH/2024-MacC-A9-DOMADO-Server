spring:
  # ?????? ?? ??
  datasource:
    url: jdbc:h2:mem:testdb  # ??? DB ??
    # url: jdbc:h2:file:./data/testdb  # ?? DB ?? (??? ???? ?? ?)
    username: sa  # ?? ????
    password:     # ?? ???? ??
    driver-class-name: org.h2.Driver
  
  # JPA ??
  jpa:
    hibernate:
      ddl-auto: create-drop  # ??? ??? ??, ??? ??
    show-sql: true           # SQL ?? ?? ??
    properties:
      hibernate:
        format_sql: true     # SQL ?? ???
    defer-datasource-initialization: true  # ??? ??? ??? ??
  
  # H2 ?? ??
  h2:
    console:
      enabled: true          # ? ?? ???
      path: /h2-console      # ?? ?? ??
  
  # SQL ??? ??
  sql:
    init:
      mode: always          # data.sql ?? ??

# ?? ??
logging:
  level:
    org.hibernate.SQL: debug            # SQL ??
    org.hibernate.type.descriptor.sql: trace  # SQL ???? ??
    com.yourpackage: debug              # ?????? ??

# ?? ??
server:
  port: 8080